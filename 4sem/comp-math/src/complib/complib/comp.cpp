// #include <iostream>

// #include <cmath>

// template <typename T> void matrix_solution(T &arr, T &sol) {
//   T::size_type len_row = arr.size();
//   T::size_type len_col = arr[0].size();

//   T pivot = arr[0][0];

//   for (T::size_type i = 1; i != len_row; ++i) {
//     for (T::size_type j = 0; j != n - 1; ++j) {
//       T mult = arr[0][j] / arr[i][j];
//       arr[i][j] -= arr[0][j] * mult;
//     }
//   }
// }
